已阅读当前项目状态。总结如下：

**项目**: Blender `.clip` 导入器的原生向量渲染

**当前焦点 — `Vector_SizePressure.clip`**（唯一未解决的阻挡项）:
- `max=226`, `mean=0.024409`, `visible=151` — 残留像素集中在大型尾部 dab，特别是 segment 26 附近
- 所有可见残留物是输出专用的硬边像素（向量图层）
- 原生 `0x31` 范围通道公式：`primary * (1.0 + (amount1 - 1.0) * secondary)`，`amount1=1.5`

**已解决的 guard**（像素精确）:
| 文件 | 状态 |
|------|------|
| `Vector_Baseline` | ✅ 像素精确 |
| `Vector_FlowPressure_50` | ✅ 像素精确 |
| `Vector_Flow_50` | ✅ 像素精确 |
| `Vector_OpacityRandom_50` | ✅ 像素精确 |
| `Vector_OpacityPressure` | ✅ 像素精确 |
| `Vector_Opacity_50` | ✅ 仅 1 个不可见字节差异 |

**下一步目标** (`AI_MEMORY.md` 第 78–85 行): 继续调查 SizePressure 中仅在大小动态硬边描边中激活的特定分支 — 在样式/绘制设置中，在发出的采样值构建中，以及当前转换/导出空间状态。

有什么需要我帮忙查看的吗？
