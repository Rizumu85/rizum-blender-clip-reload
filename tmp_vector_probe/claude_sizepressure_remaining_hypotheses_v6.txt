以下为剩余假设，已确认的路径/标志已在旁注中列出：

- **尾部片段 26 的 dab 密度过高。** 七个原生 dab（220-226）中的几个可以单独移除，且不会丢失参考像素（分析 L1590）。如果 CSP 在该尾部以略大的间距步长进行采样，可能会少生成 2-3 个 dab，从而在密集重叠区域消除 ~120 个额外像素。需要检查的是 `0x1431FFEC0`（样条弧长计算）和 `0x143200C90`（样条族的 `t` 搜索），具体针对*仅*该片段触及的短跨度/边界条件。*已确认路径：* `0x1422CC1E0` 的 `state+8` 残留传递、`PWVectorSplineCurve` 六点查找、零初值残留均正确，且通用的 `t` 搜索/长度扫描结果为中性（L1544-L1620）。

- **残留边界偏移在尾部产生一个额外的 dab。** 到片段 26 的进场残留为 `1.079194244`。片段长度浮点舍入（`0x1422C96D0` 或样条变体）中的极小差异，可能会将片段 26 中的所有 dab 中心偏移约 ~1px，从而导致在尾部覆盖一个额外的硬边缘 span。这里需要的是片段到片段的残留审计，而非通用扫描：`0x1422CC1E0` 在片段耗尽时将 `v42 - segment_length` 写回；如果原生片段长度相差 ~0.6-1.0px，片段 26 的 dab 数量/位置就会发生变化。*已确认路径：* `sub_1425A4B20` 以 `seed, 0, 0.0` 初始化，spacing 使用*当前*大小的步长（L1582），且 `+0x68` 样条 `t` 插值器正确（L1524）。

- **在 `0x142640150` 中，硬跨度 `sqrt(r² - dy²) - 0.4` 与尾部半径相交，在整数跨度处产生舍入差异。** 尾部 dab 的半径在 ~9.69–18.28 范围内。通用的半径量化扫描结果为中性（L1594），但可能仅有几个特定的 `(dy, r)` 对在 python-double 与原生 SSE `sqrtsd` + `cvttsd2si` 之间产生整数跨度差。需要进行逐行跨度验证，以比较原生跨度边界与导入器计算结果，仅针对片段 26 的 7 个 dab。*检查点：* 在 `0x142640150` 处，原生 `x0 = int(cx - span)`，`x1 = int(cx + span)`，包含；反汇编确认了指令选择（`sqrtsd` 后接 `cvttsd2si`）。

**如果以上均无进展：** 下一步需要逐个片段比较完整的原生 dab 流（中心、半径、片段长度），针对所有 233 个 dab，使用现有的 IDA 会话进行。6 点样条的反向投影看起来是正确的，因为基线/Flow/OpacityRandom/OpacityPressure 均为精确，但仅比较片段 26 的流，专门定位位置/间距差异。一个输出所有 233 个 importer dab `(cx, cy, r, segment_id)` 的 JSON 即可以 DA 反汇编为基础，重现原生 `0x1422CC1E0` 循环的采样/间距顺序。
